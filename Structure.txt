url-shortener/
├── main.go             # Entry point: Initializes app and routes
├── .env                # Secret environment variables (DB URLs, API Keys)
├── .gitignore          # Files to ignore (node_modules, .env, binary files)
├── go.mod              # Go module definition
├── go.sum              # Dependency checksums
│
├── api/                # Handlers / Controllers
│   └── url_handler.go  # Logic for shortening and redirecting
│
├── internal/           # Private code (Business logic & Helpers)
│   ├── database/       # DB initialization (Mongo & Redis)
│   │   ├── mongodb.go
│   │   └── redis.go
│   ├── models/         # Data structures (Structs)
│   │   └── url.go
│   └── utils/          # Helper functions (Random string gen, validation)
│       ├── base62.go
│       └── validator.go
│
├── middleware/         # Custom Fiber middleware
│   └── auth.go         # (Optional) if you add user accounts later
│
└── pkg/                # Reusable public code (Swagger docs, etc.)
    └── swagger/        # Generated API documentation files